What file contains the RSA keys of systems that are authorized to connect via SSH?

.ssh/authorized_keys
File is located in user's home directory.
It specifies the SSH keys that can be used for logging into the user account for which the file is configured.